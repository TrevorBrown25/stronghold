# Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional: use a custom identifier if you track multiple campaigns
NEXT_PUBLIC_SUPABASE_CAMPAIGN_ID=primary
# Optional: override the table name that stores campaign state
NEXT_PUBLIC_SUPABASE_TABLE=campaign_states

# Shared password required to unlock editing in the DM dashboard
NEXT_PUBLIC_EDIT_PASSWORD=TMG
